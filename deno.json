{
  "tasks": {
    "test": "deno test --allow-all --no-check test/**/*.test.ts; exit 0",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["require-await"]
    }
  }
}
